# 版本紀錄

## 1.0.5 — 2026-09-23

### 新增
- 蜘蛛接龍、新接龍：每一欄最上面那串能整疊搬的牌維持原本亮度，它以下不成串的面朝上牌稍微變暗，一眼看出上面那串接得多長

### 修正
- 牌堆下方的數字（剩餘張數、重翻次數、收了幾串）被牌堆疊起來的陰影蓋住，窄牌時幾乎看不到。數字改到所有牌之上的獨立一層，字級至少 11px
- 疊在同一位置的牌只留最上面那張的陰影，牌堆底下不再出現一大塊黑色
- 首頁卡片的「繼續」與「新局」包成一組，窄螢幕上跟難度選項擠不下時一起換到下一行並靠右，不會再被拆成兩行

## 1.0.4 — 2026-09-18

### 修正
- 加到主畫面後，轉成橫向再轉回直向，整個畫面的觸控位置會往上偏一個狀態列的高度，底部按鈕點不到、「回選單」完全失效。原因是 `default` 狀態列樣式下橫向會隱藏狀態列、視圖跟著改變大小，iOS 27 的 WebKit 觸控座標沒有跟上。改用 `black-translucent` 可避開，但 iOS 26 起系統會在狀態列鋪上無法關閉的 Liquid Glass 模糊、把標題列洗白，所以維持 `default`，改在轉向後等尺寸穩定、切換一次 `viewport-fit` 逼 WebKit 重算視口
- iOS 26 起獨立模式的狀態列顏色不再讀 `theme-color`，改為取樣最上方 fixed 元素的底色；加一條 1px 的取色元素讓狀態列與標題列同色

### 變更
- 「左手模式」改為「慣用手」（右手 / 左手），語意是牌堆放在慣用手那一側。原本預設牌堆在左上、開左手模式反而跑到右上，兩種都離該用的拇指最遠。現在預設右手，牌堆在右；左手則在左，橫向時按鈕列也跟著換到左邊
- 金字塔直向底排重排：牌堆在慣用手那一側的最外邊，抽牌往內翻到棄牌堆，湊 13 小表在另一側才不會被拇指遮住
- 舊設定有開左手模式的會自動對應到「左手」，其餘視為右手

## 1.0.3 — 2026-09-18

### 新增
- 每款遊戲的標題列顯示局號，選單新增「複製局號」（大字顯示純數字並自動複製）與「輸入局號」，可和朋友玩同一副牌
- 新接龍局號上限從 32000 放寬到 1,000,000，與 Windows 相同；前 100 萬局中有 8 局無解

### 修正
- 輸入局號只收數字並限制位數，輸入無效時錯誤顯示在框下方、對話框不關閉，Enter 可直接開始
- 對話框開著時可以雙指放大、關掉後卻縮不回來：全站 `touch-action` 改為 `pan-x pan-y`，只允許捲動，對話框、提示列都不能再縮放
- 資料備份文字框字級改為 16px 並在 viewport 加上 `maximum-scale=1`，iPhone 點進輸入框不再放大整頁
- 快速連點按鈕也會攔下瀏覽器的雙擊放大，改由程式補發點擊
- 資料備份：iPhone 貼上時鍵盤會把開頭 `SOL1` 自動改成小寫而匯入失敗。文字框關掉自動修正、前綴不分大小寫、忽略夾雜的空白換行；匯入失敗時留在對話框，在文字框下方說明原因與補救方式
- 資料備份：「複製」在沒有剪貼簿 API 的環境（例如區網 http）改用舊式複製，一鍵複製整段；對話框內的文字框可正常長按選取

## 1.0.2 — 2026-09-17

### 新增
- 首頁標題列在「接龍合集」旁加上 App 圖示
- README 加入圖示與 iPhone 樣式的遊戲畫面截圖
- `npm run screenshots`：用無頭 Chrome 擷圖並套上 iPhone 外框，產生 README 截圖

## 1.0.1 — 2026-09-17

### 修正
- 「回到這局開頭」改用發牌時的初始快照，長局存檔後不會回到中途
- 經典接龍分數不再低於 0
- 花色符號加上文字呈現選擇符，Android 不會顯示成彩色 emoji
- iPhone 主畫面圖示改為滿版不透明，四角不再出現黑邊；Android 另附 maskable 版本
- 牌加上邊框、角標改為數字與花色並排，疊牌時每張都看得清楚
- 四種桌面顏色加深，`theme-color` 與標題列同色，狀態列不再斷成兩截
- 獨立模式改用 `default` 狀態列，避開 iOS 26+ 玻璃模糊；底部安全區在獨立模式加上限，不再空一大段
- 對話框高度改用 `dvh`；滑鼠拖曳加上指標捕捉

### 新增
- 存檔改為「種子 + 動作日誌」，重開後可一路復原到開頭，單局存檔僅數 KB；重播失敗時自動退回最終快照
- 無路可走時顯示常駐橫幅，提供復原、回到開頭、發新局；經典、蜘蛛、新接龍補上卡死判定
- 首頁「新局」按鈕加上確認；長按「復原」可回到這局開頭
- 新局發牌動畫
- 資料備份：統計、設定與進行中牌局可匯出成文字、貼回還原
- 提示不再建議把剛搬的牌搬回去
- 版本號集中到 `version.js`；獨立模式隱藏「加入主畫面」提示
- 單元測試頁 `tests.html`、圖示產生器 `scripts/icons.html`

## 1.0.0 — 2026-09-17

首次發布。

- 五款遊戲：經典接龍、蜘蛛接龍、新接龍、金字塔、三峰
- 難度選項：翻 1/3 張、1/2/4 花色、暫存格 4/3/2/1、重翻無限/2 次/不可
- 新接龍使用 Microsoft 牌局編號（1–32000）
- 拖曳與點選自動移動、復原、提示、收牌
- 每款各自存檔、統計與連勝紀錄
- 直向與橫向排版、左手模式、大字模式、桌面與牌背顏色
- 金字塔「湊 13」對照表
- Service Worker 離線快取，可加入 iPhone 主畫面
